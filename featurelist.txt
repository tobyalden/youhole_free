YOUHOLE FEATURE LIST:

- All successful video IDs are stored and blacklisted
- Buffering (Two IFrames)
- Responsive button (animates on click & disappears / reappears based on mouse movement)
- Keyboard controls
- Better foreign language support (e.g., using dictionaries rather than nonsense phrases)
- Don't always pick the first video
- Mobile / Non-Chrome support (use v2 API)
- Anchor & shrink frame to fit in window
- When video finishes, hide title and related videos.